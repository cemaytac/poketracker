<%- include('../partials/header') %>

<h2>User List</h2>
<ul>Registered Users:
  <% users.forEach(u => { %>
  <a href="/users/<%= u._id %>">
    <li><%= u.name %></li>
  </a>
  <% }) %>
</ul>

<%- include('../partials/footer') %>